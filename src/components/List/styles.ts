import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    listEmptyTitle: {
        color: '#a1a1a1',
        fontSize: 14,
        textAlign: 'center',
        fontWeight: 'bold',
    },
    listEmptyText: {
        color: '#a1a1a1',
        fontSize: 14,
        textAlign: 'center',
    },
    container: {
        marginTop: 60
    },
        lista: {
        width: 300,
        marginTop: 30,
    },
});